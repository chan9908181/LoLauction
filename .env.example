# MongoDB Atlas Configuration
# Get your connection string from MongoDB Atlas Dashboard
# 1. Go to https://cloud.mongodb.com/
# 2. Create a new cluster (free tier available)
# 3. Create a database user
# 4. Whitelist your IP address (or use 0.0.0.0/0 for development)
# 5. Get connection string from "Connect" button
MONGODB_URI=mongodb+srv://username:password@cluster-name.mongodb.net/auction-app?retryWrites=true&w=majority

# API Configuration
API_URL=http://localhost:8000
NEXT_PUBLIC_API_URL=http://localhost:8000

# Authentication Secrets
NEXTAUTH_SECRET=your-nextauth-secret-key
NEXTAUTH_URL=http://localhost:3000
JWT_SECRET=your-jwt-secret-key

# Production Configuration (for deployment)
# MONGODB_URI=mongodb+srv://prod-user:prod-password@prod-cluster.mongodb.net/auction-app-prod?retryWrites=true&w=majority
# API_URL=https://your-domain.com
# NEXT_PUBLIC_API_URL=https://your-domain.com
# NEXTAUTH_URL=https://your-domain.com
